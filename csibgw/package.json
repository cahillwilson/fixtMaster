{
  "name": "csibgw",
  "version": "1.0.0",
  "description": "Local server which acts as customer backend for REST calls through CSI-BGW APIs.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0"
  }
}
